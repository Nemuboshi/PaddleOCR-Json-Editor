# Paddle JSON Editor

Paddle JSON Editor edits PaddleOCR PP-StructureV3 layout JSON files.
The application previews blocks, edits content, searches, downloads images, and exports JSON or Markdown.

## Development

```bash
pnpm --dir frontend install --frozen-lockfile
wails dev
```


## Build

```bash
wails build
```

